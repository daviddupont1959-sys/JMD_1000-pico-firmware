Given a raspberry pi pico W with a motion sensor connected to pin 1, and this LED configuration:
leds = {
    "WIFI": machine.Pin(8, machine.Pin.OUT),
    "BlueTooth": machine.Pin(7, machine.Pin.OUT),
    "Motion": machine.Pin(6, machine.Pin.OUT),
    "ALERT": machine.Pin(5, machine.Pin.OUT),
    "Working": machine.Pin(9, machine.Pin.OUT)
}

give me micropython code to satisfy these requirements
1. As soon as possible after power up turn on the status LED to indicate activity
2. On startup get the time from the internet and account for timezone and daylight savings as needed.
	If connection to internet could not be made, retry every 10 minutes
3. Every sunday at 2:05 AM refresh the time to ensure daylight savings is correct.
	If connection to internet could not be made, retry every 10 minutes
4. Read a configuration file named config.txt to get the following information
	{
		"alert_time":28800,
		"email_addresses":["david.dupont1959@gmail.com"],
		"interval_time":600,
		"location":"4700 Rolling Water Dr.",
		"logLevel":"DEBUG",
		"phone_numbers":["6035983717@tmomail.net"],
		"wifi_password":"network password",
		"wifi_ssid":"network ID"
	}
	Time values are in seconds.
	logLevels can be:
		LEVELS = {
			"DEBUG": 10,
			"INFO" : 20,
			"ALERT": 30,
			"ERROR": 40,
		}

5. Send an email to everyone on the email list indicating the device location and that the device has started and also the firmware revision.
6. Configure and listen on the bluetooth low energy for a connection from a device.
7. If a bluetooth connection is made, send the configuration as a JSON string over the bluetooth
8. enter the main loop checking the motion detector status every second.
9. If no motion has been detected in the configured alert_time, send email and text alerts to all the addresses on the list.
