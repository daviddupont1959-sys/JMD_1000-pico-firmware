'''
Motion Detector by Just Me Designs (JMD)
    This project is to create a motion detector which can send email messages to configured addresses in the event that no motion is detected after
     a configured time.
    It is meant to monitor that a person is actively moving in their environment, and alert loved ones if it appears they might have become unable to move
    It should be placed in a location that cannot detect a person if they have fallen in range of the detector,
     and could trigger it with arm movements or something like that.  The recommendation would be to plug it into a counter outlet in the kitchen area.
     The counter would block the detector from detecting the person on the floor even directly below the sensor.
    
    Two sets of email addresses can be configured
        A. Monitor email.  These addresses will receive an email daily to let the users know that the monitor is still operating.
        B. Alert email.  These addresses should be connfigured to generate text messages to the loved ones and will be sent when
           the configured timeout has occurred.  Most wireless carriers provide a special email address that will immediately send
           a test message to the subscriber.  For T-Mobile the address would look like this '6035983717@tmomail.net'
    
    2024/12/26 - Eliminate multi-threaded operation.
        Coding has been going on for a while, and I was trying to get the detecting code to run in a separate thread so that
        other things could be done in the main thread.  I came to the realization that there is nothing else that might need
        to be done once the device is configured.  Therefore I've decided to make a rather significant change to the code, and
        set it up to allow BlueTooth copnnections in the first 30 seconds after power on in order to allow for configuration,
        then enter into the monitor state, from which the only end is to remove power.
    
    2025/01/14 - Major restructure to clean up the code, and allow download of log files via bluetooth.
        Bluetooth connections are now allowed at any time.
        I might also provide for firmware updates over bluetooth. (This presents the problem of what to do if the update breaks the device.)
        
    2025/02/08 - Major development is complete.  Started work on making the code more robust especially with regards to internet connection stuff.
    
    2025/03/19 - added code to track and indicate firmware revision. Version 1.00
    
    2025/03/25 - Version 1.01
                 Fix time update code so it only fetches time ONCE a week from the internet.  Previous version repeated fetch several times.
                 
    2025/03/29 - Version 1.02
                 Changed order of log file archives so that log_1.txt is most recent, and log_5.txt is oldest
    
    2025/04/02 - Version 1.03
                 Added TST bluetooth command to test email and text messaging to make sure it's configured correctly.
        
    2025/04/17 - Version 1.04
                    Made a change so that if the internet connection fails the device wuill retry every 5 minutes
                    To support this I moved the creastion of the internet class to a separate definition
                    Main code changes made around line #374, the original code was replaced with a call to this new def around line #327
                    That code is around line #260
    
    2025/04/21 - Version 1.05
                    Discovered that the update function needs work.  Also want to add a way to get the current version when connected using the app.
                    
    2025/05/13 - Version 1.06
                    Noticed that when internet connection doesn't work, and time gets set to 2000, it needs to retry every so often,  I'm thinking every 15 minutes?
                    Also noticed that weekly time update is happening on Monday when it was supposed to be on Sunday
                    
    2025/06/08 - Version 1.07
                    Trying to add enough log file debug messages to help get this working next time I get a chance to try it at Dad's house.
                    2025/07/27 - confirmed working, but text messages aren't being sent.
    
    2025/07/28 - Version 1.08
                    T-Mobile decided to stop supporting the email to SMS gateway.
                    Need to come up with a long term solution, but for now.....
                    Changed sending alerts from text to email on alert, and alert cancelled.
                    
    2025/09/04 - Version 2.00
                    Modified code to use MQTT for communication across the Internet.
    
    2026/05/30 - Version 2.01
                    After a long break to digitize some film, revisiting this project.
                    Updated MQTT topic structure. (With AI help)
                    Updated handler for MQTT requests. (With AI help)

    2026/06/23 - Version 2.02
                    Bug fixes identified during AI code review. (With AI help)
                    - Added missing 'import ota_update' (OTA would crash on first use)
                    - Fixed NameError: 'topic_ack' -> topics["topic_ack"] in handle_update
                    - Fixed termMsg() call missing topic argument (would crash on internet time failure)
                    - Fixed handle_sync_time to use setRTC() correctly instead of non-existent set_time()
                    - Fixed motion_flag logic: added ever_seen_motion flag to prevent false alert on boot
                    - Fixed iLogFile: string+int concat crash and wrong .py extension in log file deletion
                    - Changed FW_REV from float to string "2.02" to avoid float representation issues
                    - Fixed iCFG: added _sanitize() to coerce numeric config values to int after
                      load or MQTT set_config update, preventing TypeError crash if string values sent
                    
                    
                    
'''
                    - Changed I/O pins as follows:
                        ----------------------------------
                        | Description  | Was    | Is     |
                        ----------------------------------
                        | Status       | 9      | 18     |
                        ----------------------------------
                        | Bluetooth    | 7      | 20     |
                        ----------------------------------
                        | WiFi         | 8      | 19     |
                        ----------------------------------
                        | Motion       | 6      | 21     |
                        ----------------------------------
                        | Alert        | 5      | 22     |
                        ----------------------------------
                        | PIR          | 1      | 28     |
                        ----------------------------------
                    - Revised I/O pins again for mechanical layout - LEDs now contiguous
                      GPIO 16-20 block at board edge, ground on pin 23:
                        ----------------------------------
                        | Description  | Was    | Is     |
                        ----------------------------------
                        | Status       | 18     | 16     |
                        ----------------------------------
                        | WiFi         | 19     | 17     |
                        ----------------------------------
                        | Bluetooth    | 20     | 18     |
                        ----------------------------------
                        | Motion       | 21     | 19     |
                        ----------------------------------
                        | Alert        | 22     | 20     |
                        ----------------------------------
                        | PIR          | 28     | 28 (unchanged) |
                        ----------------------------------
                    - Enhanced get_config command: now accepts an optional dot-path in the
                      message body (e.g. "thresholds.inactivity_alert") to return a single
                      config value instead of the full config. Empty payload still returns
                      the full config. Invalid/unknown paths now return a clear error on
                      response/error instead of failing silently.
                    - Fixed set_config: merge_dict now validates against the existing config
                      schema instead of blindly creating new keys. Sending a flat/incorrect
                      key (e.g. "inactivity_alert" instead of "thresholds.inactivity_alert")
                      now returns ERR: unknown config key '...' instead of silently creating
                      an orphaned, confusing top-level entry in config.txt. Also rejects
                      attempts to overwrite a nested setting with a single scalar value.
                      The update is validated against a trial copy first, so a rejected
                      key cannot leave the live config partially modified.
                    - Fixed init_RTC: added try/except around time_from_internet call to
                      catch OSError -2 (DNS resolution failure). Previously a transient DNS
                      failure during startup would crash the device even though WiFi and MQTT
                      were both connected. Now logs the error and falls back to birthday
                      sentinel so the main loop NeedUpdate logic retries automatically.
                    - Added lamp_test() function: sequential LED test runs at startup
                      to verify all LEDs are working before monitoring begins
                    - Added ble_manager.un_advertise() at end of allow_bt() to cleanly
                      stop BLE advertising after the 15 second window expires
                    - Improved inactivity alert message: threshold now displayed as
                      HH:MM:SS instead of a raw decimal hours value
                    - Removed unused iEmail.py and umail.py from lib folder
                    - Renamed email_message_text to alert_message_text throughout
                      Detect.py to reflect that notifications are now sent via ntfy
                      push notifications, not email
                    - Final I/O pin assignments (updated to match physical wiring):
                        ----------------------------------
                        | Description  | GPIO   | HW Pin |
                        ----------------------------------
                        | Status       | 20     | 26     |
                        ----------------------------------
                        | WiFi         | 19     | 25     |
                        ----------------------------------
                        | Bluetooth    | 18     | 24     |
                        ----------------------------------
                        | Motion       | 17     | 22     |
                        ----------------------------------
                        | Alert        | 16     | 21     |
                        ----------------------------------
                        | PIR          | 28     | 34     |
                        ----------------------------------
                        | Ground       | N/A    | 23     |
                        ----------------------------------
                    - Removed unused library files: InternetHandler.py, Wifi.py,
                      iClk_WithTimeZone.py (dead code from earlier versions, never
                      imported in v2.02)
                    - Fixed BLE LED not lighting on connection: added explicit
                      ble_manager.led_control("on") in allow_bt() when a connection
                      is detected, to handle race condition between IRQ handler and
                      polling loop
                    - Fixed check_bt: added 30-second idle timeout to handle apps
                      (like MIT App Inventor) that don't send a clean BLE disconnect.
                      Idle timer resets on any received data, and forces a disconnect
                      after 30 seconds of inactivity rather than waiting indefinitely.
                    - Added ntfy push notification on device boot/restart so family
                      members are notified when the device comes online
                    - Fixed WiFi drop crash (test 6.1): OSError EHOSTUNREACH was
                      propagating up and crashing the device when network dropped.
                      Fixed by:
                      - termMsg now catches OSError in addition to SystemExit
                      - publish_time() call in main loop wrapped in try/except
                      - Alert SET and CLEAR publish calls in main loop wrapped
                        in try/except so a network drop during alert processing
                        no longer crashes the device
                    - Added network.hostname("JMD-1000") in iNet.py connect() method
                      before wlan.active(True) so router displays device name instead
                      of MAC address
                    - Removed BLE idle timeout from check_bt — no longer needed
                      since App Inventor app now disconnects cleanly via Connect
                      by index method
                    - Fixed WiFi drop recovery: MQTT OSError handler now checks and
                      reconnects WiFi before attempting MQTT reconnection. Previously
                      MQTT reconnect was attempted while WiFi was still broken, causing
                      an endless retry loop. Also improved is_connected() in iNet.py
                      to use wlan.status() == 3 alongside isconnected() for more
                      reliable WiFi state detection.
                    - Fixed commands not working after extended runtime: two root causes
                      found and fixed:
                      1. connect_mqtt() was not re-subscribing after reconnection, so
                         after any MQTT reconnect the device could publish but not receive
                         commands until a full reboot
                      2. check_msg() only caught OSError but not MQTTException — broker
                         dropping the subscription raised MQTTException which was silently
                         swallowed, breaking command reception while publishing continued
                      - Added periodic ping() every 60 seconds to keep broker connection
                        alive on long-running sessions and prevent silent subscription loss

    2026/07/06 - Version 2.03
                    Version bump to establish clean baseline for long-term testing
                    and pre-deployment validation. All changes since 2.02 initial
                    release are consolidated here:

                    Firmware fixes:
                    - Fixed init_RTC: OSError -2 (DNS failure) no longer crashes
                      device on startup — falls back to birthday sentinel and retries
                    - Fixed termMsg: now catches OSError in addition to SystemExit
                      so network failures during publish are logged, not crashed
                    - Fixed publish_time: checks and reconnects WiFi before attempting
                      MQTT reconnection when network is unreachable
                    - Fixed check_msg: now catches MQTTException in addition to OSError
                      so broker-side subscription drops are handled gracefully
                    - Fixed connect_mqtt: now re-subscribes after every reconnection
                      so commands work after any MQTT reconnect, not just after reboot
                    - Added periodic ping() every 60 seconds to keep broker connection
                      alive on long-running sessions
                    - Fixed set_config: schema validation now rejects unknown keys,
                      flat keys instead of nested paths, and scalar values where a
                      nested object is expected — all rejected atomically with no
                      partial updates saved
                    - Fixed get_config: now accepts optional dot-path for single value
                      retrieval (e.g. "thresholds.inactivity_alert") with clear error
                      on invalid paths
                    - Fixed iCFG: added _sanitize() to coerce numeric config values
                      to int after any load or MQTT update
                    - Fixed BLE LED not lighting on connection
                    - Fixed check_bt duplicate while loop (introduced and fixed)
                    - Removed BLE idle timeout — no longer needed with corrected
                      App Inventor connect-by-index method
                    - Added ntfy push notification on device boot/restart
                    - Added lamp_test() sequential LED test on startup
                    - Added ble_manager.un_advertise() at end of allow_bt()
                    - Improved inactivity alert message to show HH:MM:SS format
                    - Added network.hostname("JMD-1000") in iNet.py for router
                      device name display
                    - Improved is_connected() to use wlan.status() == 3 for more
                      reliable WiFi state detection
                    - Added WiFi reconnection logic before MQTT reconnect in main
                      loop OSError handler

                    Code cleanup:
                    - Removed unused library files: iEmail.py, umail.py,
                      InternetHandler.py, Wifi.py, iClk_WithTimeZone.py
                    - Renamed email_message_text to alert_message_text throughout
                    - Changed FW_REV from float to string
                    - Final I/O pin assignments consolidated (see pin table in 2.02)
                    - Removed unused motion_interval config field from config.txt,
                      iCFG.py defaults and _sanitize() — field was never referenced
                      in Detect.py
                    - Removed iClk.py.bak from package
                    - Refactored load_config in iCFG.py: iCFG.py defaults are now
                      the authoritative schema for config.txt. load_config uses a
                      selective merge that only applies keys that exist in the schema,
                      silently dropping orphaned keys (e.g. motion_interval) and
                      preserving default values for any missing keys. This means:
                      - Orphaned config keys disappear automatically on next load
                      - New config keys added to iCFG.py defaults appear automatically
                        after an OTA update without needing a set_config command
                      - config.txt is always a clean subset of the schema

    2026/07/08 - Version 2.04
                    - Added handle_version command: publish to JMD_1000/command/get_version
                      to retrieve current firmware version on JMD_1000/state/version
                      and confirmation on JMD_1000/response/ack
                    - Added topic_get_version to command topics and dispatcher
                    - Fixed duplicate topic_version key in topics dictionary
                    - Refactored load_config in iCFG.py: iCFG.py defaults are now
                      the authoritative schema — orphaned keys (e.g. motion_interval)
                      are silently dropped on load, new keys appear automatically
                    - Removed motion_interval from iCFG.py defaults and config.txt
                    - Removed iClk.py.bak from package
